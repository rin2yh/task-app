# Git rules

- `git push --force` および `git push --force-with-lease` は **禁止**。実行しない。
- `git rebase`・`git reset --hard`・`git commit --amend` で push 済みのコミットを書き換えることも **禁止**。
- 履歴を整理したい場合は、新規コミットを積むか、別ブランチで PR を作り直す。
